"use client";
import { useEffect, useState } from "react";
import { Bell, RefreshCw } from "lucide-react";
export default function AdminNotifications(){const [data,setData]=useState<{pendingPurchases:number;pendingUsers:number;pendingWithdrawals:number;unread:number}|null>(null);const [loading,setLoading]=useState(true);async function load(){setLoading(true);try{const r=await fetch('/api/admin/notifications',{credentials:'include',cache:'no-store'});const d=await r.json();if(r.ok)setData(d)}finally{setLoading(false)}}useEffect(()=>{load()},[]);return <main className="min-h-full text-white"><div className="mb-8 flex items-end justify-between"><div><p className="text-sm uppercase tracking-[.25em] text-cyan-300/70">Control center</p><h1 className="mt-2 text-3xl font-bold">Notifications</h1><p className="mt-1 text-sm text-slate-400">Operational items that need attention.</p></div><button onClick={load} className="inline-flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-sm hover:bg-white/10"><RefreshCw size={16}/> Refresh</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[['Pending Purchases',data?.pendingPurchases??0,'Review payment proofs'],['Unverified Users',data?.pendingUsers??0,'Check new accounts'],['Pending Withdrawals',data?.pendingWithdrawals??0,'Review payout requests'],['Unread Alerts',data?.unread??0,'System notifications']].map(([title,value,desc])=><div key={String(title)} className="rounded-2xl border border-white/10 bg-white/[.04] p-5 backdrop-blur-xl"><div className="flex items-center justify-between"><span className="text-sm text-slate-400">{title}</span><Bell size={18} className="text-cyan-300"/></div><div className="mt-3 text-3xl font-bold">{loading?'—':value}</div><div className="mt-1 text-xs text-slate-500">{desc}</div></div>)}</div><div className="mt-6 rounded-3xl border border-white/10 bg-white/[.04] p-6"><h2 className="font-semibold">Recommended queue</h2><div className="mt-4 grid gap-3 sm:grid-cols-3"><a href="/admin/purchases" className="rounded-xl border border-white/10 bg-black/20 p-4 hover:border-cyan-400/30"><div className="font-medium">Payment verification</div><div className="mt-1 text-xs text-slate-500">Open pending purchases</div></a><a href="/admin/withdrawals" className="rounded-xl border border-white/10 bg-black/20 p-4 hover:border-cyan-400/30"><div className="font-medium">Withdrawal review</div><div className="mt-1 text-xs text-slate-500">Process payout requests</div></a><a href="/admin/referrals" className="rounded-xl border border-white/10 bg-black/20 p-4 hover:border-cyan-400/30"><div className="font-medium">Referral network</div><div className="mt-1 text-xs text-slate-500">Inspect referral balances</div></a></div></div></main>}
