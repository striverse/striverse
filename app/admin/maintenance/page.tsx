"use client";import {useEffect,useState} from "react";import {Wrench} from "lucide-react";export default function Maintenance(){const[on,setOn]=useState(false);const[msg,setMsg]=useState('');useEffect(()=>{fetch('/api/admin/maintenance').then(r=>r.json()).then(d=>setOn(d.settings?.maintenanceMode==='true'))},[]);async function save(){const r=await fetch('/api/admin/maintenance',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({maintenanceMode:on,emergencyFinancialLock:on?'false':'false'})});const d=await r.json();setMsg(d.message||'Saved')}return <main className="space-y-6 text-white"><header><p className="text-xs uppercase tracking-[.3em] text-cyan-300">Developer only</p><h1 className="mt-2 text-3xl font-black">Maintenance & Feature Flags</h1><p className="text-sm text-slate-400">Control maintenance mode and emergency operational flags.</p></header><section className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="flex items-center gap-3"><Wrench className="text-amber-300"/><div><div className="font-bold">Maintenance Mode</div><div className="text-xs text-slate-400">Temporarily restrict app operations.</div></div><input className="ml-auto" type="checkbox" checked={on} onChange={e=>setOn(e.target.checked)}/></div><button onClick={save} className="mt-5 rounded-xl bg-amber-500 px-5 py-3 font-bold text-black">Save</button>{msg&&<span className="ml-3 text-sm text-cyan-200">{msg}</span>}</section></main>}
