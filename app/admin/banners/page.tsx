"use client";import {useEffect,useState} from "react";import {Megaphone,Plus} from "lucide-react";
export default function Banners(){const[b,setB]=useState<any[]>([]);const[f,setF]=useState({title:"",description:"",imageUrl:"",buttonText:"",buttonLink:"",target:"ALL",frequency:"ONCE",isActive:false});async function load(){const r=await fetch('/api/admin/banners');const d=await r.json();if(r.ok)setB(d.banners||[])}useEffect(()=>{load()},[]);async function add(){const r=await fetch('/api/admin/banners',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});if(r.ok){setF({title:"",description:"",imageUrl:"",buttonText:"",buttonLink:"",target:"ALL",frequency:"ONCE",isActive:false});load()}}return <main className="space-y-6 text-white"><header><p className="text-xs uppercase tracking-[.3em] text-cyan-300">Developer only</p><h1 className="mt-2 text-3xl font-black">Banners & Notifications</h1><p className="text-sm text-slate-400">Floating/special banners with target, frequency and active controls.</p></header><section className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="grid gap-3 md:grid-cols-2">{Object.entries(f).map(([k,v])=><input key={k} placeholder={k} value={String(v)} onChange={e=>setF({...f,[k]:k==='isActive'?e.target.checked as any:e.target.value})} className="rounded-xl bg-black/30 p-3"/> )}</div><button onClick={add} className="mt-4 inline-flex items-center gap-2 rounded-xl bg-cyan-500 px-5 py-3 font-bold"><Plus size={16}/> Create Banner</button></section>{b.map(x=><div key={x.id} className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="flex items-center justify-between"><div><div className="font-bold">{x.title}</div><div className="text-xs text-slate-400">{x.target} · {x.frequency}</div></div><span>{x.isActive?'ACTIVE':'OFF'}</span></div></div>)}</main>}
