"use client";import {useEffect,useState} from "react";import {Wallet} from "lucide-react";export default function Finance(){const[d,setD]=useState<any>(null);useEffect(()=>{fetch('/api/admin/finance').then(r=>r.json()).then(x=>{if(x.success)setD(x.finance)})},[]);return <main className="space-y-6 text-white"><header><p className="text-xs uppercase tracking-[.3em] text-cyan-300">Developer only</p><h1 className="mt-2 text-3xl font-black">Finance</h1><p className="text-sm text-slate-400">Financial overview and payout exposure.</p></header><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{[['Users',d?.users||0],['Approved USDT',`${d?.approvedUSDT||0} USDT`],['Approved STV',`${Number(d?.approvedSTV||0).toLocaleString()} STV`],['Pending USDT',`${d?.pendingUSDT||0} USDT`],['Paid Withdrawals',`${d?.paidWithdrawals||0} USDT`]].map(([k,v])=><div key={String(k)} className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><Wallet size={18} className="text-cyan-300"/><div className="mt-3 text-xs text-slate-400">{k}</div><div className="mt-1 text-2xl font-black">{v}</div></div>)}</div></main>}
