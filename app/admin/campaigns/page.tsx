"use client";import {useEffect,useState} from "react";import {Target,Plus} from "lucide-react";
export default function Campaigns(){const[c,setC]=useState<any[]>([]);const[f,setF]=useState({name:"",description:"",startAt:"",endAt:"",incentiveUSDT:0,isActive:false});async function load(){const r=await fetch('/api/admin/campaigns');const d=await r.json();if(r.ok)setC(d.campaigns||[])}useEffect(()=>{load()},[]);async function add(){const r=await fetch('/api/admin/campaigns',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});if(r.ok){setF({name:"",description:"",startAt:"",endAt:"",incentiveUSDT:0,isActive:false});load()}}return <main className="space-y-6 text-white"><header><p className="text-xs uppercase tracking-[.3em] text-cyan-300">Developer only</p><h1 className="mt-2 text-3xl font-black">Campaigns & Incentives</h1><p className="text-sm text-slate-400">Manage limited-time campaigns and special Partner incentives.</p></header><section className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="grid gap-3 md:grid-cols-2">{[['name','Campaign name'],['description','Description'],['startAt','Start ISO date'],['endAt','End ISO date']].map(([k,p])=><input key={k} placeholder={p} value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})} className="rounded-xl bg-black/30 p-3"/>)}<input type="number" placeholder="Incentive USDT" value={f.incentiveUSDT} onChange={e=>setF({...f,incentiveUSDT:+e.target.value})} className="rounded-xl bg-black/30 p-3"/><label className="flex items-center gap-2"><input type="checkbox" checked={f.isActive} onChange={e=>setF({...f,isActive:e.target.checked})}/> Active</label></div><button onClick={add} className="mt-4 inline-flex items-center gap-2 rounded-xl bg-violet-500 px-5 py-3 font-bold"><Plus size={16}/> Create Campaign</button></section>{c.map(x=><div key={x.id} className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="flex items-center justify-between"><div><div className="font-bold">{x.name}</div><div className="text-xs text-slate-400">{new Date(x.startAt).toLocaleString()} → {new Date(x.endAt).toLocaleString()}</div></div><span className="text-violet-200">{x.incentiveUSDT} USDT</span></div></div>)}</main>}
